# .github/workflows/deploy.yml

name: Deploy

on:
  push:
    branches:
      - main  # Adjust this to your deployment branch

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Log in to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend image
        run: |
          docker build -t yourusername/backend:latest -f backend/Dockerfile ./backend
          docker push yourusername/backend:latest

      - name: Build and push frontend image
        run: |
          docker build -t yourusername/frontend:latest -f frontend/Dockerfile ./frontend
          docker push yourusername/frontend:latest

      - name: Deploy to your environment
        run: |
          # Add your deployment steps here, such as pulling and running Docker images on your server
